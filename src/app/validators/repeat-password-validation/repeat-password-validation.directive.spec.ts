import { RepeatPasswordValidationDirective } from './repeat-password-validation.directive';

describe('RepeatPasswordValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new RepeatPasswordValidationDirective();
    expect(directive).toBeTruthy();
  });
});
